#lang scheme/base

(require (file "../content-base.ss")
         (file "ui/home-page.ss"))

; Controllers ------------------------------------

(define-controller (home request)
  (send home-page set-posts! (find-all (sql:select #:from (sql:entity entity:post) #:limit 10)))
  (send home-page respond #:forward? #t))
