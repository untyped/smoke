#lang scheme

(require "../../test-base.ss")

; Tests ------------------------------------------

(define/provide-test-suite send-tests
  
  ; current-site and current-page are set after send/suspend

  ; current-site and current-page are set after send/suspend/dispatch

  )
  
  